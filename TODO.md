# Changes This Session

- Finished HourBreakout Strategy
- Finished MiddleRange Strategy (just need to check if all trades actually match, report gave minimal differences between the old and new versions)
- Applied multiple strategy initialization and input per strategy to the StrategyManager

# To Do Next

- Confirm MiddleRange strategy is placing trades correctly (also confirm the reset variables)
- Start developing the PUSH Simulation with OOP.
